{% extends "/account/base-myaccount.html.twig" %}

{% block title %}Ma liste de favoris{% endblock %}
{% block stylesheets %}
{{parent() }}
<link rel="stylesheet" href="{{asset('build/favorite.css')}}">
{% endblock %}
{% block body %}

{% for favoriteProduct in favoriteProducts %}
<div class="product">
    Name: {{ favoriteProduct.product.name }}</br>
    Year: {{ favoriteProduct.product.year }}</br>
    Price: {{ favoriteProduct.product.price }}€</br>
    Favorite_id: {{ favoriteProduct.id }}</br>
    <a href=" {{ path("favorite_add", {'product_id': favoriteProduct.product.id}) }}"><button>Add</button></a>
    <a href=" {{ path("favorite_remove", {'favoriteProduct_id': favoriteProduct.id}) }}"><button>Remove</button></a>
    <a href=" {{ path("cart_add", {'product_id': favoriteProduct.product.id}) }}"><button>Add Cart</button></a>
</div>
{% endfor %}

{% endblock %}